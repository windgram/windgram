using Microsoft.AspNetCore.Identity;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using Windgram.Identity.ApplicationCore.Domain.Entities;

namespace Windgram.Identity.STS.Extensions
{
    public static class UserManagerExtensions
    {
        //public const string EmailConfirmationCodePurpose = "PhoneNumberConfirmation";
        //public static async Task<string> GenerateEmailConfirmationCodeAsync(this UserManager<UserIdentity> userManager, UserIdentity user)
        //{ 
        //    var token = await userManager.GenerateUserTokenAsync(user, TokenOptions.DefaultEmailProvider);
        //    return token;
        //}
    }
}
